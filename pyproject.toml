[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eml2md"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["markdownify"]

[project.scripts]
eml2md = "eml2md.cli:main"

[tool.setuptools.packages.find]
include = ["eml2md*"]
